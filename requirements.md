# 要件定義書 — Trello 風タスク管理アプリ

## 1. プロジェクト概要

Trello に着想を得た、カンバン方式のタスク管理 Web アプリケーション。  
複数のリスト（列）とカード（タスク）を直感的に操作でき、進捗を視覚的に管理できる。

---

## 2. 技術スタック

| 項目 | 選定技術 |
|------|----------|
| フレームワーク | React 18 |
| 言語 | TypeScript |
| ビルドツール | Vite |
| ドラッグ&ドロップ | @hello-pangea/dnd |
| スタイリング | CSS Modules（またはTailwind CSS） |
| データ永続化 | LocalStorage |

---

## 3. 機能要件

### 3.1 ボード

- アプリ起動時にデフォルトのボードを1つ表示する
- ボードのタイトルを表示・編集できる

### 3.2 リスト（列）

| # | 機能 |
|---|------|
| L-1 | リストを新規作成できる（タイトル入力） |
| L-2 | リストのタイトルをインライン編集できる |
| L-3 | リストを削除できる（確認なし） |
| L-4 | リストをドラッグ&ドロップで並び替えられる |
| L-5 | リストは横スクロールで複数表示できる |

### 3.3 カード（タスク）

| # | 機能 |
|---|------|
| C-1 | カードをリスト内に新規作成できる（タイトル入力） |
| C-2 | カードのタイトルをインライン編集できる |
| C-3 | カードを削除できる |
| C-4 | カードをドラッグ&ドロップでリスト内で並び替えられる |
| C-5 | カードをドラッグ&ドロップで別リストへ移動できる |

### 3.4 データ永続化

| # | 機能 |
|---|------|
| D-1 | ボードの状態（リスト・カード）を LocalStorage に自動保存する |
| D-2 | ページリロード後もデータが復元される |

---

## 4. 非機能要件

| 項目 | 要件 |
|------|------|
| レスポンシブ | デスクトップブラウザを主対象とする |
| ブラウザ対応 | Chrome 最新版 |
| オフライン動作 | LocalStorage のため完全オフライン動作可能 |
| ユーザー認証 | 不要（シングルユーザー前提） |
| サーバー | 不要 |

---

## 5. 画面構成

```
┌─────────────────────────────────────────────────────┐
│  [ボードタイトル]                                    │
├─────────────────────────────────────────────────────┤
│ ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────┐ │
│ │ リストA  │  │ リストB  │  │ リストC  │  │ ＋   │ │
│ │──────────│  │──────────│  │──────────│  │ 追加 │ │
│ │ カード1  │  │ カード3  │  │          │  └──────┘ │
│ │ カード2  │  │          │  │          │           │
│ │          │  │          │  │          │           │
│ │ ＋ 追加  │  │ ＋ 追加  │  │ ＋ 追加  │           │
│ └──────────┘  └──────────┘  └──────────┘           │
└─────────────────────────────────────────────────────┘
```

---

## 6. データモデル

```typescript
type Card = {
  id: string;
  title: string;
  createdAt: string; // ISO 8601
};

type List = {
  id: string;
  title: string;
  cards: Card[];
};

type Board = {
  id: string;
  title: string;
  lists: List[];
};
```

---

## 7. ディレクトリ構成（予定）

```
trello-app/
├── src/
│   ├── components/
│   │   ├── Board/
│   │   ├── List/
│   │   └── Card/
│   ├── hooks/
│   │   └── useLocalStorage.ts
│   ├── types/
│   │   └── index.ts
│   ├── App.tsx
│   └── main.tsx
├── index.html
├── vite.config.ts
└── package.json
```

---

## 8. 今後の拡張候補（スコープ外）

- カード詳細モーダル（説明文・期限日・ラベル）
- 複数ボード対応
- チェックリスト
- ユーザー認証 / クラウド同期
- 検索・フィルター機能
